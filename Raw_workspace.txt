Add the following to your environment variables (e.g. in your .env file):

RAZORPAY_KEY_ID=dummy_key_id
RAZORPAY_KEY_SECRET=dummy_key_secret

#Some Codespace templates allow you to run Docker.

✅ #You can run MongoDB with Docker (if supported in your Codespace):

$  docker run -d -p 27017:27017 --name my-mongo mongo:latest

🌐 or 


$    docker run -d -p 27017:27017 --name my-mongo mongo:latest


✅  your command:

$  docker exec -it my-mongo mongosh


✅ If that still doesn’t work, try:

$ docker exec -it my-mongo /usr/bin/mongosh


✅ 1. Connect to mongo shell (via Docker):

$  docker exec -it my-mongo mongosh


✅Restart Your MongoDB Container
$ docker start my-mongo

or 
$ docker exec -it my-mongo mongosh



Step 2: ✅ Create the custom "master" role before assigning it

db.users.insertOne({ username: "master", role: "master", password: "India@12345" })

✅ or 

db.createRole({
  role: "master",
  privileges: [
    {
      resource: { db: "infocart", collection: "" },
      actions: [ "find", "insert", "update", "remove" ]
    }
  ],
  roles: []
})


🔸 Step 3: ✅ Now create the user with that role

db.createUser({
  user: "master",
  pwd: "India@12345",
  roles: [
    { role: "master", db: "infocart" }
  ]
})


4) updateMasterPassword();
$  node scripts/updateMasterPassword.js



### You can also use the built-in Source Control tab in Codespaces to stage, commit, and push — no terminal needed!

If you’re working on a fork or feature branch like patch_1, make sure you're on the correct branch before pushing:

$  git branch
$  git checkout patch_1


